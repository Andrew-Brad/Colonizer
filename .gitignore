# This repo is PUBLIC. Guard against committing host-specific or secret material.

# Host-specific SSH blocks (hostnames, usernames) -- local only.
config.d/
os/windows/ssh/config.d/

# Private key material, in any form.
id_*
*.pem
*.ppk
*_rsa
*_ed25519
*_ecdsa
known_hosts
known_hosts.old

# 1Password / secret tooling local state
.op/
*.env
.env.*
!.env.example
