aws --version

aws configure

# Type in key and secret
# Default region is us-east-2
# Format can be json, text, table, etc

aws sns list-topics
aws sns list-subscriptions
aws sns list-subscriptions-by-topic --topic-arn <value>
